# Ping死活監視

## usage

1. bundle install
1. chgange ip addres in mvserver.rb
1. run ruby script
    ```shell
    $ ruby mvserver.rb
    ```
1. enjoy mv server

## on OSX only

this program uses `say` command
